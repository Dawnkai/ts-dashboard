/**
 * Colors to insert where CSS variable is needed (e.g. style property: style={{ color: COLORS_CSS.blue }})
 */
export const COLORS_CSS = {
	blue: "var(--blue)",
	red: "var(--red)",
	green: "var(--green)",
	yellow: "var(--yellow)",
	orange: "var(--orange)",
};

/**
 * Colors to insert where HEX value is needed (e.g. color property of come sort)
 */
export const COLORS = {
	blue: "#3b86ff",
	red: "#ff5e5e",
	green: "#0fc177",
	yellow: "#f7d31d",
	orange: "#ff8c42",
};
